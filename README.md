# React + Vite

Backend → npm run server
(corre tu server.js, conecta a SQL Server y DBF).

Frontend → npm run dev
(levanta Vite con React para mostrar la interfaz).

Por eso tienes que abrir dos terminales y correr cada comando.

